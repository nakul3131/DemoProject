@model IEnumerable<DemoProject.Services.ViewModel.Management.Master.EvaluationSectorContentItemViewModel>

@using DemoProject.Services.ViewModel.Configuration

@{
    // Make Proper Menu Parameter
    HeaderPartialModel headerPartialModel = new HeaderPartialModel("ESCRIN");
}

<!doctype html>
<html lang="en-US">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width,initial-scale=1.0" />

    <title>EvaluationSectorContentItem - Index </title>
</head>
<body>
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-12">
                @Html.Partial("Header", headerPartialModel)
            </div>
        </div>
        <br />

        <div class="card">
            <div class="card-body no-footer">
                <table id="example" class="display table table-striped table-hover dt-responsive nowrap cell-border">
                    <thead>
                        <tr>
                            <th>SR.</th>
                            <th>Name Of EvaluationSection</th>
                            <th>Entry Date</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        @{
                            foreach (var item in Model)
                            {
                                <tr class="hoverlightblue">
                                    <td></td>
                                    <td>@item.NameOfEvaluationSection</td>
                                    <td>@item.MakerEntryDateTime</td>
                                    <td>
                                        <button class="btn btn-link" onclick="location.href='@Url.Action("Amend","EvaluationSectorContentItem",new {@item.EvaluationSectionId })'">Amend </button>
                                    </td>
                                </tr>
                            }
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</body>
</html>

